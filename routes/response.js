const express = require('express');
const router  = express.Router();

module.exports = () => {
  router.get("/:id", (req, res) => {
  });

  router.post("/:id", (req, res) => {
  });

  return router;
};
